# Extra dependencies for the custom FastAPI dashboard (server.py + web/).
# Not needed for the Streamlit app, which is what Streamlit Community Cloud
# deploys, so these are kept out of requirements.txt to keep that build lean.
#
# Install with:  pip install -r requirements.txt -r requirements-web.txt
# Run with:      uvicorn server:app --reload --port 8000

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0
